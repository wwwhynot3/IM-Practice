server:
  port: 8082
  #  tomcat:
  #    uri-encoding: utf-8
  servlet:
    encoding:
      force: true
      charset: utf-8
#      enabled: true

spring:
  mvc:
    servlet:
      load-on-startup: 10
    log-request-details: true

  profiles:
    active: dev
    group:
      "dev": rocketmq, mongodb

logging:
  level:
    web: debug